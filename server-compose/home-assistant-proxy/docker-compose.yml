name: home-assistant-proxy

services:
  ha-proxy:
    image: alpine/socat
    container_name: ha-proxy
    restart: unless-stopped
    command:
      - tcp-listen:8123,fork,reuseaddr
      - tcp:${HOME_ASSISTANT_HOST}:8123
    ports:
      - "8123:8123"
